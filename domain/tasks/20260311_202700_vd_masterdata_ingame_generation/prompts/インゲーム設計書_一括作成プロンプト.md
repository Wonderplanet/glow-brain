# インゲーム設計書（design.md）一括作成プロンプト

## 目的

`vd-ingame-design-creator/` 配下のブロックフォルダを確認し、`design.md` が未作成のブロックを対象に、スキル `/vd-masterdata-ingame-design-creator` を使ってインゲーム設計書を作成してください。

---

## 作成対象の作品一覧

対象は以下の **16作品** です。各作品につき **Normal / Boss の2ブロック**を作成します。

| 作品 | 作品ID |
|------|--------|
| SPY×FAMILY | `spy` |
| ダンダダン | `dan` |
| 姫様"拷問"の時間です | `gom` |
| チェンソーマン | `chi` |
| 株式会社マジルミエ | `mag` |
| 怪獣８号 | `kai` |
| 2.5次元の誘惑 | `yuw` |
| 魔都精兵のスレイブ | `sur` |
| サマータイムレンダ | `sum` |
| 地獄楽 | `jig` |
| タコピーの原罪 | `tak` |
| 【推しの子】 | `osh` |
| 幼稚園WARS | `you` |
| 100カノ | `kim` |
| ふつうの軽音部 | `hut` |
| あやかしトライアングル | `aya` |

---

## 作業手順

### Step 1: 対象ブロックの特定

`vd-ingame-design-creator/` 配下のフォルダを確認し、`design.md` が存在しないブロックをリストアップしてください。

- `vd_all/` は除外する
- 対象はブロック種別フォルダ（例: `vd_kai_normal_00001/`、`vd_kai_boss_00001/` など）のみ
- 上記16作品 × 2種別（Normal/Boss）= **最大32ブロック**が対象になりえる
- すでに `design.md` が存在するブロックはスキップする

### Step 2: 設計書の一括作成

対象ブロックを **最大3つずつ並行して** スキルを実行してください。

- ユーザーへの確認は不要です
- 各ブロックの作品IDとブロック種別（Normal / Boss）の情報だけでインゲーム設計書を完成させてください
- 3ブロック分が完了したら、次の3ブロックに進んでください

#### スキル呼び出し方法

```
/vd-masterdata-ingame-design-creator 作品ID={作品ID} ブロック種別={Normal または Boss}
```

**引数の例**:
- `vd_kai_normal_00001` → 作品ID: `kai`、ブロック種別: `Normal`
- `vd_osh_boss_00001` → 作品ID: `osh`、ブロック種別: `Boss`

---

## 参照データ

敵キャラクターデータは以下のCSVを参照してください:

```
vd-ingame-design-creator/vd_all/data/
├── MstEnemyStageParameter.csv   # 全VD作品の敵パラメータ一覧
├── MstEnemyCharacter.csv        # 敵キャラクター基本情報
└── MstEnemyCharacterI18n.csv    # 敵キャラクター名称（多言語）
```

---

## 完了条件

全ての対象ブロックについて `design.md` が生成されたら作業完了です。

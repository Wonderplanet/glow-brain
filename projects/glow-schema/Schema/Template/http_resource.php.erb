<?php

/**
 * ----------------------------------------------------------------------
 * This file was output by schema builder.
 * If the file exists, running the schema builder will not overwrite it.
 *
 * Please edit this file if you want to make any customizations.
 *
 * このファイルはschema builderによって出力されました。
 * ファイルが存在する場合、schema builderを実行しても上書きされません。
 *
 * カスタマイズを行う場合は、このファイルを編集してください。
 * ----------------------------------------------------------------------
 */

declare(strict_types=1);

namespace App\Http\Resources\Api\<%= schema["base"]["name"] %>;

use App\Http\Resources\Api\<%= schema["base"]["name"] %>\Base\Base<%= schema["name"] %>Resource;

/**
 <%- if schema["description"] then -%>
 * <%= schema["description"] %>
 <%- end -%>
 */
class <%= schema["name"] %>Resource extends Base<%= schema["name"] %>Resource
{
    /**
     * @return $this
     */
    public function build(): self
    {
        return $this;
    }
}

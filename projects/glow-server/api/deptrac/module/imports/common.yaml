deptrac:
  layers:
    - name: Common Entity
      collectors:
        - type: directory
          value: ./app/Domain/Common/Entities/.*

  ruleset:
    Common Entity: ~

  formatters:
    graphviz:
      groups:
        Common:
          - Common Entity

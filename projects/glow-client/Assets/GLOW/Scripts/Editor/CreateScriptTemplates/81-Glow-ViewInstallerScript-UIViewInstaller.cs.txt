using Zenject;
using UIKit.ZenjectBridge;
using GLOW.Scenes.#SCRIPTNAME#.Presentation;
using GLOW.Scenes.#SCRIPTNAME#.Domain;

namespace GLOW.Scenes.#SCRIPTNAME#.Application
{
    public class #SCRIPTNAME#ViewInstaller : Installer
    {
        public override void InstallBindings()
        {
            Container.BindViewWithKernal<#SCRIPTNAME#ViewController>();
            Container.BindInterfacesTo<#SCRIPTNAME#Presenter>().AsCached();
            Container.Bind<#SCRIPTNAME#UseCase>().AsCached();
        }
    }
}

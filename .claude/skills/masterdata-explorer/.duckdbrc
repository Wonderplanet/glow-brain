-- DuckDB初期化ファイル for GLOW Masterdata Explorer
-- 使い方: duckdb -init .claude/skills/masterdata-explorer/.duckdbrc

-- 便利な設定
.mode csv
.headers on

-- よく使うテーブルのビュー（必要に応じてコメントアウトを外す）
-- CREATE VIEW units AS
--   SELECT * FROM read_csv('projects/glow-masterdata/MstUnit.csv', AUTO_DETECT=TRUE, nullstr='__NULL__')
--   WHERE ENABLE = 'e';
--
-- CREATE VIEW events AS
--   SELECT * FROM read_csv('projects/glow-masterdata/MstEvent.csv', AUTO_DETECT=TRUE, nullstr='__NULL__')
--   WHERE ENABLE = 'e';
--
-- CREATE VIEW series AS
--   SELECT * FROM read_csv('projects/glow-masterdata/MstSeries.csv', AUTO_DETECT=TRUE, nullstr='__NULL__')
--   WHERE ENABLE = 'e';

-- ヘルプメッセージ
SELECT '=== GLOW Masterdata Explorer (DuckDB) ===' AS message;
SELECT '' AS message;
SELECT 'CSVの読み込み方法:' AS message;
SELECT '  read_csv(''projects/glow-masterdata/MstUnit.csv'', AUTO_DETECT=TRUE, nullstr=''__NULL__'')' AS message;
SELECT '' AS message;
SELECT '例:' AS message;
SELECT '  SELECT * FROM read_csv(''projects/glow-masterdata/MstUnit.csv'', AUTO_DETECT=TRUE, nullstr=''__NULL__'')' AS message;
SELECT '    WHERE ENABLE = ''e'' AND rarity = ''UR'' LIMIT 10;' AS message;
SELECT '' AS message;
SELECT 'テーブル名はCSV形式（例: MstUnit.csv, MstEvent.csv）で指定してください。' AS message;
SELECT '詳細は .claude/skills/masterdata-explorer/references/duckdb-query-examples.md を参照。' AS message;
